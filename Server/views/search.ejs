<!DOCTYPE html>
<html>

<body>
    <h3>웹 단어 검색 시스템</h3>
    <form action="/search" method="post">
        <p>
            <table>
                <tr>
                    <th>Engine</th>
                    <th>Word</th>
                </tr>
                <tr>
                    <td>
                        <select class="selectpicker" name="engineList">
            <option value="naver">naver</option>
            <option value="daum">daum</option>
            <option value="all">all</option>
        </select>
                    </td>
                    <td>
                        <select name="wordList">
            <% name.forEach(function(w) { %>
                <option value="<%= w %>">
                    <%= w %>
                </option>
            <% }); %>
        </select>
                    </td>
                    <td>
                        <input class="btn btn-primary" type="submit" value="View" />
                    </td>
                </tr>
            </table>
        </p>
    </form>
    <div class="contact-menu">
        <a href="/create"><input type="button" value="Register" /></a>
    </div>
</body>

</html>
